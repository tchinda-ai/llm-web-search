# SearXNG minimal configuration for local Docker use
use_default_settings: true

server:
  secret_key: "llm-web-search-searxng-secret-key-local"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json
  default_lang: "en-US"
  timeout_limit: 15.0

engines:
  # Google's HTML structure changes frequently and breaks SearXNG's parser.
  # Re-enabled because we now have a proxy to prevent blocking.
  - name: google
    disabled: false

  # google_news uses a more stable API endpoint (less prone to breakage)
  - name: google news
    disabled: false

  - name: bing
    disabled: false

  - name: brave
    disabled: false

  - name: duckduckgo
    disabled: false

  - name: wikipedia
    disabled: false
